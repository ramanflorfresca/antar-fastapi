fastapi==0.135.1
uvicorn==0.41.0
python-dotenv==1.0.0
pyswisseph==2.10.3.2
supabase==2.28.0
httpx==0.28.1
h2>=4.1.0  # HTTP/2 for httpx — required by antar_engine/push_sender.py (APNs)
pydantic==2.12.5
pydantic-settings>=2.1.0
python-multipart==0.0.6
pytz==2024.1
apscheduler==3.10.4
openai==2.26.0
python-dateutil==2.8.2
anyio==4.12.1
distro==1.9.0
sniffio==1.3.0
tqdm==4.66.2
resend
httpx

stripe
razorpay
timezonefinder==8.2.2
weasyprint
# [sdk-compat 2026-09-05] PINNED. Was `>=0.40.0` (unpinned) — a redeploy pulled a
# newer SDK that rejected the `temperature` kwarg, so EVERY Claude call failed and
# silently fell back to DeepSeek (225 failures in one log window). Pin to the
# declared floor this code was written+tested against so a redeploy can't silently
# upgrade the SDK again. The llm_log retry stays as a belt-and-suspenders net.
# To move up: bump deliberately, verify `[startup] anthropic SDK version` + no
# "SDK rejected 'temperature'" logs, then commit the new pin.
anthropic==0.40.0
uvloop>=0.19.0
httptools>=0.6.0
cachetools>=5.3.0
slowapi>=0.1.9
geoip2==4.8.0
babel==2.13.1
# [iap/push 2026-06-29] explicit pins for store IAP verification + FCM push.
# Present transitively today, but pinned so Railway can't drop them.
PyJWT>=2.3.0
cryptography>=3.4.8
requests>=2.25.0
